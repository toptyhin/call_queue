# Stand replaces this file at check time. Only these four are required by the contract.
WEBHOOK_SECRET=dev-webhook-secret
JWT_SECRET=dev-jwt-secret-change-me-32bytes!!
CRM_URL=http://mocks:8090/crm
PROVIDER_URL=http://mocks:8090

# Optional local-dev settings (not required by the stand contract).
WEB_ORIGIN=http://localhost:5173
COOKIE_SECURE=false
DEV_TOKEN_ENABLED=true
